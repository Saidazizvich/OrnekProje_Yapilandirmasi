@model OrnekProje_Entity.Models.Category  

@{
   var title = "Update";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Update</h1>
<form method="post">
    <div class="row p-3 border">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="CategoryId" />
        @if (Model.CategoryId != 0)
        {
            var update = Model.CategoryId;    
                 
               
        }
        <div class="col-12 border-bottom">
            <h2 class="text-primary">@title</h2>
        </div>

        <div class="col-8 pt-4">
            <div class="form-group row">
                <div class="col-4">
                    <label asp-for="CategoryName"></label>
                </div>

                <div class="col-4">
                    <input asp-for="CategoryName" class="form-control">
                    <span asp-validation-for="CategoryName" class="text-danger"></span>
                </div>

                <div class="col-4">
                    <label asp-for="CategoryId"></label>
                </div>
                <div class="form-group row">
                    <div class="col-4 offset-4">
                        @if (Model.CategoryId != 0)
                        {
                            <button type="submit" class="btn btn-warning form-control">Created Category</button>
                        }
                        else
                        {
                            <button type="submit" class="btn btn-warning form-control">Created Category</button>

                        }
                    </div>

                    <div class="col-4">
                        <a asp-action="Index" class="btn btn-primary form-control">List reaperat place</a>
                    </div>

                </div>

            </div>
        </div>
    </div>
</form>

@section Scripts{
	<partial name="_ValidationScriptsPartial" />
}

