@model IEnumerable<OrnekProje_Entity.Models.Publisher>

<br />
<br />

<div class="border p-4 bg-white">
    <div class="row">
        <div class="text-info">
            <h2 class="">Writer List</h2>
            <div class="col-2 text-right">
                <a asp-action="Index" class="btn btn-info text-danger">Create New Writer</a>
            </div>
        </div>
        <br />
        <div>
            @if (Model.Count() > 0)
            {
                <table class="table table-striped border">
                    <tr class="table-secondary">
                        <th>Id</th>
                        <th>Name</th>
                        <th>Location</th>
                    </tr>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.PublisherId</td>
                            <td>@item.Name</td>
                            <td>@item.Location</td>
                            <td>
                                <a class="btn btn-primary text-danger" asp-action="Update" asp-route-Id="@item.PublisherId"></a>
                            </td>
                            <td>
                                <a class="btn btn-primary text-danger" asp-action="Insert" asp-route-Id="@item.PublisherId"></a>
                            </td>
                            <td>
                                <a class="btn btn-primary text-danger" asp-action="Delete" asp-route-Id="@item.PublisherId"></a>
                            </td>
                        </tr>
                    }
                </table>

            }
        </div>
    </div>
</div>


